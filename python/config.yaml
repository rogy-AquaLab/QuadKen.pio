tcp:
  debug_mode: on  # on または off
  debug_options:
    show_timestamp: true
    show_colors: true

controller:
  type: "logi_x"  # "pro_con", "logi_x", "logi_d"

main:
  interval: 0.1  # メインループの実行間隔（秒）